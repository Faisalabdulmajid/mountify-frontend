// src/components/common/Spinner/Spinner.js

import React from "react";
import "./Spinner.css"; // Kita akan membuat file CSS ini selanjutnya

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
